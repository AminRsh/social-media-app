import { LikeInfo } from "@/lib/types"

interface LikeButtonProps {
    postId: string,
    intialState: LikeInfo 
}
export default function LikeButton({postId,  intialState}: LikeButtonProps) {
    return <div className="">

    </div>
}
